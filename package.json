{
  "name": "addraf",
  "version": "1.2.1",
  "description": "Autofill fields with address information looked up from Japanese postal code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "tsc ./src/main.ts --removeComments --outDir ./dist --target es2017 && terser -c --passes=2 -m -o ./dist/main.min.js -- ./dist/main.js && eslint --ignore-path .eslintignore --ext .js,.ts . --fix"
  },
  "keywords": [
    "address",
    "postcode",
    "zipcode",
    "form"
  ],
  "author": "Sean Hay",
  "license": "MIT"
}
